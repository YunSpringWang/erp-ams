一、背景

框架：spring+springMVC+mybatis+maven
jdk:1.8
mysql:mysql-5.0.96-winx64
tomcat:apache-tomcat-8.5.16
前端技术：Layui(参考地址--http://www.layui.com/demo/；快速上手文档：http://www.layui.com/doc/)

一、登录模块

注册>>>>>>>对于用户的密码等敏感信息采用MD5进行加密。
单点登录登出
验证码

二、用户数据管理

用户添加，删除/批量删除,修改，查询/多条件查询
用户数据导入和导出。可用EXCEL和PDF格式导出用户数据，也支持用Excel文档导入用户数据

三、角色数据维护

角色信息维护，角色信息搜索，为角色分配通用管理后台权限，为角色分配接入应用系统权限，角色权限查询，角色反查用户，树形设置界面
系统中先建立角色，角色中再分配权限，在角色的成员中加入用户。当然会有一些用户的权限大同小异，如果不想在系统中创建太多角色可以给用户分配权限。用户最终的权限取角色权限和用户权限的集合。

四、用户组（机构）

系统的用户组管理，方便管理系统下的人员结构，同时可直接浏览组织架构下的人员信息。提供用户组信息维护，用户组信息搜索，用户组人员浏览等功能
组织机构、多级树型显示，各级部门从属关系一目了然操作便捷

五、模块也可以称之为菜单

动态加载属性菜单栏，可对菜单进行增删改查，不同的角色对应不同的菜单界面
禁用

六、权限管理
权限分配模式：
1）角色权限分配，权限分配的便捷性。
2）用户权限分配，权限分配的灵活性。
某某主体 在 某某领域 有 某某权限 

1，主体可以是用户，可以是角色，也可以是一个部门

2， 领域可以是一个模块，可以是一个页面，也可以是页面上的按钮

3， 权限可以是“可见”，可以是“只读”，也可以是“可用”(如按钮可以点击)

权限控制类型：
1）模块权限 采购员有采购模块权限和库存查看模块权限。
2）按钮权限 采购员能点击新增按钮新增采购单，采购经理能点击审核按钮审核采购单。
3）数据权限 每个业务员只能看到属于自己的客户资料。
4）字段权限 会计可以看到库存报表中的所有字段，仓库管理员不能看到报表中的金额字段。
5）文件权限采购部的文件只有总经理和采购部成员有权限


七、日志
登陆日志、操作日志、系统异常日志

八、数据库管理
在前端页面查看数据库信息，数据库备份
数据库需定期备份（每天备份一次），备份文件格式为backup_yyyyMMdd，数据库被破坏时，利用最新的备份文件进行恢复。

九、代码生成器根据需要生成界面和后台代码

十、字典
对常用数据加入字典，如：城市

十一、统计
十二、系统基本信息查看
显示服务器信息
	服务器计算机名、IP地址，域名，端口，本文所在文件夹，操作系统，系统所在文件夹
显示用户的登录次数、上次登录的IP地址、上次登录的时间
十三、流程管控

概念模型(CDM Conceptual Data Model)

物理模型（PDM,Physical Data Model）

面向对象的模型（OOM Objcet Oriented Model）

业务模型（BPM Business Process Model）
需要的表：
（w_表名）

用户表(user_base)，角色表(role)，机构表(organization)，权限表(Authority)，菜单表(menu)，日志表(logs)，字典表(dictionary)

用户表>>>用户ID，姓名，性别，年龄，手机号，邮箱（基本信息）

	登录账号，登录密码，密保问题，密保答案，登录IP，登录时间，上次登录IP，登录次数，注册时间，MAC地址，系统类型。

角色表>>>角色ID，父级角色ID，角色名称，创建时间，描述

组织机构表>>>机构ID，父级机构ID，机构名称，创建时间，描述(description)

权限表>>>权限ID，父级权限ID,权限名称，权限描述

菜单表>>>菜单（节点）ID，父节点ID，菜单（节点）名，节点类型，URL，controller，节点图标icon，资源编码，状态，描述

日志表>>>日志ID，日志类型，操作人ID，操作内容，操作时间

用户登录日志：编号，登录用户ID，登录账号，真实姓名，日志描述，IP地址，MAC地址，系统类型，记录日期

系统异常日志：主键ID，操作人,远程访问主机IP、异常所在类名、异常出现的方法名称、异常类型、异常发生时间，异常的详细内容，是否查看处理（已查看，未查看，已处理，未处理）

字典表>>>TypeTable(typeID/code,typeName)【主表，用来记录字典表表名信息】；
	DataTable（typeID,DataId,DataName）【从表，记录所有字典表数据信息】
（如性别（男，女，其它）、婚姻状态（未婚，已婚，离异），城市，学历（小学、中学、高中、本科...），种族（汉族、回族、满族...）、账户状态(正常,冻结,注销)）



项目目录结构

com
  common
	entity
	mapper
	dao
  service
	login
  	user
 	role	
	organization
	authority
	menu
	logs
	util
  controller  
	login
  	user
 	role	
	organization
	authority
	menu
	logs
	util


1、用户注册
	判断用户名是否可用（注：后台代码呈现，页面未呈现）
	对注册的密码进行加密（MD5）保存（注：后台代码呈现，页面未呈现）
	注册成功提示是否跳转至登录页面，是则保存登录信息session并跳转，否，返回登录界面（未实现）
2、登录
	实现验证码
	登录验证通过，添加session，添加登录log
	System.out.println("本机的域名/IP = " + InetAddress.getLocalHost());
	System.out.println("本机的域名 = " + InetAddress.getLocalHost().getHostName());
	System.out.println("本机的IP = " + InetAddress.getLocalHost().getHostAddress());
	跳转至首页
3、主页
	加载菜单树，进行首页显示